# Arrays, Strings and Structs

This lesson teaches the concrete memory layouts behind common source-level data
structures: contiguous arrays, terminated strings, length-prefixed strings, and
struct fields at fixed offsets.
