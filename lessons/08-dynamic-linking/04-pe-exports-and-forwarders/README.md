# PE Exports and Forwarders

This lesson teaches the other side of PE imports: how DLLs publish functions via
the export directory, how name and ordinal lookup work, and why a Windows export
may forward to another DLL instead of pointing at code in the exporting image.
